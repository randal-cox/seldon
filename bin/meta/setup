#!/usr/bin/env bash
# setup the virtual environment for project seldon

python3.9 -m pip install virtualenv
python3.9 -m virtualenv venv
source venv/bin/activate
python3.9 -m pip install -r requirements.txt
source bin/meta/source
